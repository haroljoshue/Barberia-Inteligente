@model Barberia.MVC.Controllers.AdminDashboardViewModel

@{
    ViewData["Title"] = "Dashboard Administrativo";
}

<div class="container-fluid py-4">

    <div class="mb-4">
        <h1 class="fw-bold">Dashboard Administrativo</h1>
        <p class="text-muted">
            Monitoreo de citas, usuarios, agente conversacional, logs del sistema y almacenamiento vectorial.
        </p>
    </div>

    <!-- RESUMEN GENERAL -->
    <h4 class="mb-3">Resumen general</h4>

    <div class="row g-3 mb-4">

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Total de citas</h6>
                    <h2 class="fw-bold">@Model.TotalCitas</h2>
                    <small>Registros ingresados</small>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Citas de hoy</h6>
                    <h2 class="fw-bold">@Model.CitasHoy</h2>
                    <small>Reservas de la fecha actual</small>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Citas pendientes</h6>
                    <h2 class="fw-bold">@Model.CitasPendientes</h2>
                    <small>Reservas aún no atendidas</small>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Total usuarios</h6>
                    <h2 class="fw-bold">@Model.TotalUsuarios</h2>
                    <small>Usuarios registrados</small>
                </div>
            </div>
        </div>

    </div>

    <!-- AGENTE CONVERSACIONAL -->
    <h4 class="mb-3">Agente conversacional</h4>

    <div class="row g-3 mb-4">

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Consultas al agente</h6>
                    <h2 class="fw-bold">@Model.TotalConsultasAgente</h2>
                    <small>Total de preguntas recibidas</small>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Consultas exitosas</h6>
                    <h2 class="fw-bold text-success">@Model.ConsultasExitosasAgente</h2>
                    <small>Respuestas procesadas correctamente</small>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Consultas con error</h6>
                    <h2 class="fw-bold text-danger">@Model.ConsultasErrorAgente</h2>
                    <small>Consultas fallidas o sin respuesta válida</small>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Tiempo promedio</h6>
                    <h2 class="fw-bold">@Model.TiempoPromedioRespuestaAgente ms</h2>
                    <small>Tiempo promedio de respuesta</small>
                </div>
            </div>
        </div>

    </div>

    <div class="row g-3 mb-4">

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Tokens promedio</h6>
                    <h2 class="fw-bold">@Model.TokensPromedioAgente</h2>
                    <small>Estimación de tokens usados</small>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Consultas sin resultados</h6>
                    <h2 class="fw-bold">@Model.ConsultasSinResultados</h2>
                    <small>Consultas sin resultados vectoriales</small>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Similitud promedio</h6>
                    <h2 class="fw-bold">@Model.SimilitudPromedioAgente</h2>
                    <small>Promedio de relevancia semántica</small>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Similitud máxima</h6>
                    <h2 class="fw-bold">@Model.SimilitudMaximaAgente</h2>
                    <small>Mayor coincidencia registrada</small>
                </div>
            </div>
        </div>

    </div>

    <!-- LOGS DEL SISTEMA -->
    <h4 class="mb-3">Logs del sistema</h4>

    <div class="row g-3 mb-4">

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Total logs sistema</h6>
                    <h2 class="fw-bold">@Model.TotalLogsSistema</h2>
                    <small>Eventos registrados por el sistema</small>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Logs exitosos</h6>
                    <h2 class="fw-bold text-success">@Model.LogsSistemaExitosos</h2>
                    <small>Operaciones completadas correctamente</small>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Logs con error</h6>
                    <h2 class="fw-bold text-danger">@Model.LogsSistemaError</h2>
                    <small>Operaciones fallidas o con excepción</small>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Latencia promedio</h6>
                    <h2 class="fw-bold">@Model.LatenciaPromedioSistema ms</h2>
                    <small>Tiempo promedio registrado</small>
                </div>
            </div>
        </div>

    </div>

    <div class="row g-3 mb-4">

        <div class="col-md-6">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h5 class="mb-3">Logs del sistema por tipo</h5>

                    <table class="table table-striped align-middle">
                        <thead>
                            <tr>
                                <th>Tipo</th>
                                <th>Cantidad</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.LogsSistemaPorTipo != null && Model.LogsSistemaPorTipo.Any())
                            {
                                foreach (var item in Model.LogsSistemaPorTipo)
                                {
                                    <tr>
                                        <td>@item.Key</td>
                                        <td>@item.Value</td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="2" class="text-muted">No hay logs del sistema registrados.</td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h5 class="mb-3">Logs del sistema por entidad</h5>

                    <table class="table table-striped align-middle">
                        <thead>
                            <tr>
                                <th>Entidad</th>
                                <th>Cantidad</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.LogsSistemaPorEntidad != null && Model.LogsSistemaPorEntidad.Any())
                            {
                                foreach (var item in Model.LogsSistemaPorEntidad)
                                {
                                    <tr>
                                        <td>@item.Key</td>
                                        <td>@item.Value</td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="2" class="text-muted">No hay entidades registradas.</td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </div>

    </div>

    <div class="row g-3 mb-4">

        <div class="col-md-12">
            <div class="card shadow-sm border-0">
                <div class="card-body">
                    <h5 class="mb-3">Últimos logs del sistema</h5>

                    <div class="table-responsive">
                        <table class="table table-hover align-middle">
                            <thead>
                                <tr>
                                    <th>Fecha</th>
                                    <th>Tipo</th>
                                    <th>Mensaje</th>
                                    <th>Entidad</th>
                                    <th>Usuario</th>
                                    <th>Latencia</th>
                                    <th>Estado</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.UltimosLogsSistema != null && Model.UltimosLogsSistema.Any())
                                {
                                    foreach (var log in Model.UltimosLogsSistema)
                                    {
                                        var badge = log.Exitoso ? "success" : "danger";
                                        var estado = log.Exitoso ? "Exitoso" : "Error";

                                        <tr>
                                            <td>@log.Fecha.ToString("yyyy-MM-dd HH:mm")</td>
                                            <td>@log.Tipo</td>
                                            <td>
                                                @if (!string.IsNullOrWhiteSpace(log.Mensaje))
                                                {
                                                    @(log.Mensaje.Length > 80
                                                                                            ? log.Mensaje.Substring(0, 80) + "..."
                                                                                            : log.Mensaje)
                                                                                }
                                            </td>
                                            <td>@log.Entidad</td>
                                            <td>@log.UsuarioId</td>
                                            <td>@log.LatenciaMs ms</td>
                                            <td>
                                                <span class="badge bg-@badge">@estado</span>
                                            </td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="7" class="text-muted">No existen logs del sistema registrados.</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>

                </div>
            </div>
        </div>

    </div>

    <!-- BASE VECTORIAL -->
    <h4 class="mb-3">Base vectorial</h4>

    <div class="row g-3 mb-4">

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Citas con vector</h6>
                    <h2 class="fw-bold">@Model.CitasConVector</h2>
                    <small>Registros almacenados en Qdrant</small>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Citas sin vector</h6>
                    <h2 class="fw-bold">@Model.CitasSinVector</h2>
                    <small>Registros sin embedding asociado</small>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Vectorización exitosa</h6>
                    <h2 class="fw-bold">@Model.PorcentajeVectorizacion%</h2>
                    <div class="progress mt-2">
                        <div class="progress-bar" role="progressbar"
                             style="width:@Model.PorcentajeVectorizacion%">
                            @Model.PorcentajeVectorizacion%
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h6 class="text-muted">Dimensión embedding</h6>
                    <h2 class="fw-bold">@Model.DimensionEmbedding</h2>
                    <small>Modelo: text-embedding-3-small</small>
                </div>
            </div>
        </div>

    </div>

    <!-- TABLAS AGENTE -->
    <div class="row g-3 mb-4">

        <div class="col-md-6">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h5 class="mb-3">Herramientas usadas por el agente</h5>

                    <table class="table table-striped align-middle">
                        <thead>
                            <tr>
                                <th>Herramienta</th>
                                <th>Cantidad</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.HerramientasUsadas != null && Model.HerramientasUsadas.Any())
                            {
                                foreach (var item in Model.HerramientasUsadas)
                                {
                                    <tr>
                                        <td>@item.Key</td>
                                        <td>@item.Value</td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="2" class="text-muted">No hay datos registrados.</td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h5 class="mb-3">Consultas por tipo</h5>

                    <table class="table table-striped align-middle">
                        <thead>
                            <tr>
                                <th>Tipo</th>
                                <th>Cantidad</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.ConsultasPorTipo != null && Model.ConsultasPorTipo.Any())
                            {
                                foreach (var item in Model.ConsultasPorTipo)
                                {
                                    <tr>
                                        <td>@item.Key</td>
                                        <td>@item.Value</td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="2" class="text-muted">No hay datos registrados.</td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </div>

    </div>

    <!-- ÚLTIMAS CONSULTAS AGENTE -->
    <div class="row g-3 mb-4">

        <div class="col-md-12">
            <div class="card shadow-sm border-0">
                <div class="card-body">
                    <h5 class="mb-3">Últimas consultas al agente</h5>

                    <div class="table-responsive">
                        <table class="table table-hover align-middle">
                            <thead>
                                <tr>
                                    <th>Fecha</th>
                                    <th>Usuario</th>
                                    <th>Tipo</th>
                                    <th>Pregunta</th>
                                    <th>Herramienta</th>
                                    <th>Tiempo</th>
                                    <th>Tokens</th>
                                    <th>Estado</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.UltimosLogsAgente != null && Model.UltimosLogsAgente.Any())
                                {
                                    foreach (var log in Model.UltimosLogsAgente)
                                    {
                                        var badge = log.ConsultaExitosa ? "success" : "danger";
                                        var estado = log.ConsultaExitosa ? "Exitosa" : "Error";

                                        <tr>
                                            <td>@log.Fecha.ToString("yyyy-MM-dd HH:mm")</td>
                                            <td>@log.UsuarioId</td>
                                            <td>@log.Tipo</td>
                                            <td>
                                                @if (!string.IsNullOrWhiteSpace(log.Pregunta))
                                                {
                                                    @(log.Pregunta.Length > 60
                                                                                            ? log.Pregunta.Substring(0, 60) + "..."
                                                                                            : log.Pregunta)
                                                                                }
                                            </td>
                                            <td>@log.HerramientaUsada</td>
                                            <td>@log.TiempoRespuestaMs ms</td>
                                            <td>@log.TokensUsados</td>
                                            <td>
                                                <span class="badge bg-@badge">@estado</span>
                                            </td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="8" class="text-muted">No existen consultas registradas.</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>

                </div>
            </div>
        </div>

    </div>

    <!-- RESERVAS Y USUARIOS -->
    <div class="row g-3 mb-5">

        <div class="col-md-6">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h5 class="mb-3">Reservas por servicio</h5>

                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Servicio</th>
                                <th>Cantidad</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.ReservasPorServicio != null && Model.ReservasPorServicio.Any())
                            {
                                foreach (var item in Model.ReservasPorServicio)
                                {
                                    <tr>
                                        <td>@item.Key</td>
                                        <td>@item.Value</td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="2" class="text-muted">No hay reservas registradas.</td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <h5 class="mb-3">Usuarios por rol</h5>

                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Rol</th>
                                <th>Cantidad</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.UsuariosPorRol != null && Model.UsuariosPorRol.Any())
                            {
                                foreach (var item in Model.UsuariosPorRol)
                                {
                                    <tr>
                                        <td>@item.Key</td>
                                        <td>@item.Value</td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="2" class="text-muted">No hay usuarios registrados.</td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </div>

    </div>

</div>